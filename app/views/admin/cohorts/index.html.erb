<div class="container">
  <div class="row">
    <div class="col-md-12">
      <% @cohorts.each do |cohort| %>
        <%= link_to "#{cohort.name} #{cohort.version}", cohort_path(cohort) %>
      <% end %>
    </div>
  </div>
</div>
