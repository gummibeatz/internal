<div class="container">
  <div class="row">
    <div class="col-md-12">
      <% @exit_tickets.each do |key, val| %>
        <a href="/exit_tickets/<%= pretty_date(key) %>
      <% end %>
    </div>
  </div>
</div>
