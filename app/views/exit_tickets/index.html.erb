<div class="container">
  <div class="row">
    <div class="col-md-12">
      <% @exit_tickets.each do |t| %>
        <p><%= link_to pretty_date(t.created_at), exit_ticket_path(t) %></p>
      <% end %>
    </div>
  </div>
</div>
