<div class="container">
  <div class="row">
    <div class="col-md-12">
      <form action="" method="">
        <div class="form-group">
          <label for="start-date">Start Date</label>
          <input type="date" name="start_date" value="" id="start-date" placeholder="mm/dd/yyyy" />
        </div>
        <div class="form-group">
          <label for="end-date">End Date</label>
          <input type="date" name="end_date" value="" id="end-date" placeholder="mm/dd/yyyy" />
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form>
    </div>
  </div>
</div>


<% if defined?(@report) %>

<h4>Attendance (percent present): </h4>
<p><%= @report.attendance_present_rate %></p>

<h4>Tardiness (percent late): </h4>
<p><%= @report.attendance_late_rate %></p>

<h4>Puncutality (percent on time): </h4>
<p><%= @report.attendance_on_time_rate %></p>

<h4>Exit Ticket Completion Rate in Range: </h4>
<p><%= @report.exit_ticket_completion_rate %></p>

<h4>Exit Ticket Accuracy Rate in Range: </h4>
<p><%= @report.exit_ticket_completion_rate %></p>

<% end %>